### \[TODO\]
*   [ ] dude, the vulkan and non vulkan parts are interacting too much. fix!
*   [ ] Add a render spec that contains all the data about rendering which the user can fetch at runtime.
*   [ ] Add more error checking to vulkan functions.
*   [ ] Need to really change the renderer. It should be more akin to the ctext renderer.
*   [ ] Make the ctext renderer not dependant on vulkan drawing commands. Id est add command buffer support.
*   [ ] where is the documentation man
*   [ ] why enabling vsync causes BIG lag in input??
*   [ ] Redo vulkan texture abstraction
*   [ ] Add a command buffering system to remove synchronization need from the user \+ reduce overhead of submitting to queues
*   [ ] add getters and setters for all iris structures.
